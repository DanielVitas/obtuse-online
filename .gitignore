# Gradle / build output
.gradle/
build/
*/build/
!src/**/build/

# Native libs fetched by copyAndroidNatives — never commit these (see CLAUDE.md)
android/libs/

# Machine-specific SDK path
local.properties

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/

# OS
.DS_Store
Thumbs.db

# Conversion scratch (safe to delete entirely)
_to_delete/

# Raw slime source sprite packs (ToffeeCraft/rvros) — processed frames live under images/, atlas is committed
android/assets/images/_slime_src/
