// Root build file. Everything the 2018 version needed from jcenter now comes from
// mavenCentral() and google(), declared in settings.gradle.
plugins {
    id 'com.android.application' version '8.4.2' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
